pipeline {
    agent any

    stages {
        stage("build") {
            steps {
                echo "building app..."
            }
        }

        stage("test") {
            steps {
                echo "testing app..."
            }
        }

        stage("deploy-staging") {
            steps {
                echo "deploying app..."
            }
        }
        
        stage("deploy-prod") {
            steps {
                echo "deploying app..."
            }
        }
    }
}
